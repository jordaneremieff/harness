{
  "name": "harness",
  "version": "0.1.0",
  "private": true,
  "description": "A pi harness with extensions and skills for governed reasoning, continuity, web search, and clipboard history.",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "test": "node --test --experimental-test-coverage --test-coverage-lines=60 --test-coverage-functions=60 --test-coverage-branches=50 \"extensions/*/*.test.mts\" \"scripts/*.test.mts\" \"skills/*/scripts/*.test.mts\"",
    "lint": "biome lint --error-on-warnings .",
    "format": "biome format --error-on-warnings .",
    "typecheck": "tsc -p .",
    "check": "node scripts/check-slices.mts",
    "worktrees": "node scripts/extension-worktrees.mts",
    "worktrees:sync": "node scripts/extension-worktrees.mts sync",
    "worktrees:status": "node scripts/extension-worktrees.mts status",
    "worktrees:hooks": "node scripts/extension-worktrees.mts install-hooks",
    "warmup:jiti": "pi --help --offline --extension extensions/brave/index.ts --extension extensions/statusline/index.ts --extension extensions/clipboard/index.ts --extension extensions/stash/index.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "pi": {
    "skills": [
      "./skills"
    ],
    "extensions": [
      "./extensions"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "@types/node": "^22.19.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@earendil-works/pi-protocol": "^0.84.1",
    "@earendil-works/pi-server": "^0.84.1"
  }
}
