# Dependencies and local installs
node_modules

# Per-slice configuration overlays — may contain local paths or secrets; never committed
*.config.json
.pi/

# OS noise
.DS_Store
*.tgz
*.zip

# Local dumps, scratch verification output
dump/
*.log
