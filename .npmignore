# Runtime dependencies are supplied by Pi or installed as peers.
node_modules

# Local session state and verification output must never enter an install artifact.
.pi/
.DS_Store
*.tgz
*.zip
dump/
*.log
